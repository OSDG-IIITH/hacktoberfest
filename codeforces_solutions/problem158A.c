#include<stdio.h>

int main()
{	int n,k,ai;
	scanf("%d %d",&n,&k);
	int count = 0;
	for(i=1;i<=n;i=i+1)
	{scanf("%d",&ai)
	if(k<=ai<+n)
	};
	while(number>=k)
	{if(number<n)
	count++;}
	printf("%d\n",count);
	return 0;
		}
